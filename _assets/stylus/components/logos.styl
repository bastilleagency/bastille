.logos
  grid('row', 'center', 'center')
  margin-top 60px
  &__logo
    width 100%
    max-width 120px
    padding 10px
    img
      width 100%
      height auto
      filter grayscale(100%)
      transition filter 200ms ease-in-out
      &:hover
        filter none
